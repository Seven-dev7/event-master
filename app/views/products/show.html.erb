<p id="notice"><%= notice %></p>
<h1>Produit numero : <%= @product.id %></h1>
<%= @product.name %><br>
<%= @product.description %><br>
<%= @product.price %><br>
<%= image_tag(@product.image.url, size: 150) if !@product.image.blank? %><br>

<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %> | <%= button_to 'Ajouter au panier', line_items_path(product_id: @product.id) %>
